var light = document.getElementsByName('light');
var dark = document.getElementsByName('dight');

function darknlight(dark,light) {
    if (dark.matches) { // If media query matches
      light.matches;
    } else if(light.matches){
      dark.matches;
    }
  }

